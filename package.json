{
  "name": "vocali-transcribe",
  "private": true,
  "packageManager": "pnpm@11.20.0",
  "scripts": {
    "web": "pnpm --dir web",
    "api": "pnpm --dir api",
    "dev": "concurrently \"pnpm web dev\" \"pnpm api dev\" "
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
